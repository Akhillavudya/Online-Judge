"""Services: the business logic that does real work for the routers.

- ``file_manager`` writes user code to disk.
- ``executor`` compiles and runs that code.
- ``ai_review`` asks Google Gemini to review the code.
"""
